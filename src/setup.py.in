from distutils.core import setup
import os

setup(
    name='glia',
    version='${PACKAGE_VERSION}',
    author='Laurent Lejeune',
    author_email='laurent.lejeune@artorg.unibe.ch',
    description='glia',
    packages=['glia'],
    package_dir={'glia': '${CMAKE_CURRENT_BINARY_DIR}'},
    package_data = {'glia': ['libglia.so']}
)
