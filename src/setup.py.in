from distutils.core import setup
import os

setup(
    name='pyglia',
    version='${PACKAGE_VERSION}',
    author='Laurent Lejeune',
    author_email='laurent.lejeune@artorg.unibe.ch',
    description='pyglia',
    packages=['pyglia'],
    package_dir={'pyglia': '${CMAKE_CURRENT_BINARY_DIR}/pyglia'},
    package_data = {'pyglia': ['libglia.so']}
)
