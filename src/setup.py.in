from distutils.core import setup
import os

setup(
    name='glia',
    version='${PACKAGE_VERSION}',
    author='Laurent Lejeune',
    author_email='laurent.lejeune@artorg.unibe.ch',
    description='glia',
    packages=['glia'],
    package_dir={'glia': '${PROJECT_SOURCE_DIR}/glia'},
    package_data = {'glia': ['libglia.so']}
)
